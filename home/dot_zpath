# add our non-platform specific directories to our path
path=(
  "$HOME/.fly/bin"
  /usr/local/{bin,sbin}
  $path
)

# add our ZSH functions to our path
fpath=(
  "$HOME/.zfuncs"
  "$HOME/.zcompcache"
  $fpath
)
